# GHG-01 · 宋立明教授观后感

**评审对象**：AI 赋能的叶轮机械多学科设计优化平台（GitHub: `sunccchengze/turbine-blade-ai-platform` · 线上: https://turbine-blade-ai-scz.pages.dev）
**评审方式**：从零开始，独立翻仓库、读代码、看网站，并**亲自复现了全部关键数字**
**评审人**：宋立明
**日期**：2026-08-03

---

## 〇、我是怎么看这个项目的

先说清楚我做了什么，免得你说我走马观花。

我假设自己对这个项目一无所知，按一个评审的正常路径走了一遍：

1. **翻 GitHub**：README → 提交历史（173 个提交）→ devlog → 训练 notebook → 后端代码 → 前端代码 → 数据文件 → 复现脚本 → 内部文档（pressure-test、final-acceptance、report-one-pager 等）；
2. **看网站**：首页完整读完，预测页/探索页访问时后端处于休眠冷启动（页面有醒目的"服务器正在从休眠中唤醒"提示，处理得专业，但演示前必须预热——你们自己的 D36 清单里也写了）；
3. **动手复现**：在干净环境里把 R²、NSGA-II、UQ 覆盖率、推理速度全部重跑了一遍，还顺手查证了 KIT 事件与 PLAID 数据集的真实性。

下面这份观后感，第 1 节是我的复现记录（这一节最重要），第 2 节是亮点，第 3 节是质疑，第 4 节是评分，第 5 节是我下次见面会问的问题。

---

## 一、我亲手验证过的（复现记录）

> 一个做 MDO 的老师，最烦听到的就是"精度 0.99"。所以我第一件事不是看你们怎么写，而是自己跑。

| # | 声称 | 我的复现结果 | 结论 |
|---|---|---|---|
| 1 | 测试集 R² = 0.9844 / 0.9561 / 0.9827 | 90/10 切分、seed 42、部署用 ONNX 模型实测：**0.9844 / 0.9561 / 0.9827** | ✅ 逐位一致 |
| 2 | README 折叠块披露：train 0.9926/0.9915/0.9944，val 0.9819/0.9451/0.9834 | 按 notebook 的真实切法（先 0.10 再 0.111，同 seed 42）重跑：**三行全部逐位一致** | ✅ 披露数字也是真的 |
| 3 | NSGA-II：η=0.9173、ṁ=21.74 kg/s、π=2.1073 | 按 requirements.txt 全锁版环境（pymoo 0.6.1 + numpy 1.26.4 + scipy 1.12.0 + onnxruntime 1.18.0）重跑：**生成的 CSV 与仓库提交逐字节一致（git diff 为空）** | ✅ 全锁版下精确可复现 |
| 4 | Pareto 解"超越全部 1,000 个训练样本" | 数据集真实最大值：η=0.8914、ṁ=20.84 kg/s；Pareto 前沿：η=0.9173、ṁ=21.74。相对训练均值 +5.40% / +11.43% | ✅ 数字成立（注意：π 2.1073 < 数据集最大 2.1167，你们没宣称 π 超越，处理得诚实） |
| 5 | UQ 名义 95% 实际覆盖 65–89% | 实测 π 89%、η 65%、ṁ 88%（n=100），平均 σ 与生产 API 的 sigma_map 完全一致 | ✅ 如实披露，没美化 |
| 6 | ONNX 单点推理 ~0.13 ms | 本机实测 **0.115 ms/次** | ✅ 数量级一致 |
| 7 | KIT 无压气机燃气轮机 303 秒 | 查证属实（2026-02-17 多家报道，KIT ITES 研究所，破 NASA 250 秒纪录；压气机耗功 ~50% 的说法与 KIT 官方表述一致） | ✅ 引子是真的 |
| 8 | PLAID / NASA Rotor 37 数据集 | Hugging Face `PLAID-datasets/Rotor37` 真实存在，1,000 组训练样本、3D RANS 压气机叶片几何变分数据 | ✅ 数据是真的 |

**第 1 节结论：这个仓库的核心数字，经得起一个较真的人逐位复现。** 在本科项目里，这不是常见品质。往下读之前，先把这个结论记住——下面的批评都是在这个前提上说的。

另外提一个我在复现中踩到的坑（也是给你们的改进建议，见第 3 节问题四）：

> 我把 pymoo 升到 0.6.2，η 变成 0.9212；onnxruntime 用 1.28，η 变成 0.9180；numpy 用 2.x，η 变成 0.9191。**只有严格按 requirements.txt 全锁版，才回到 0.9173 并逐字节一致。** seed 42 只在"同一环境"内成立——这个口径你们 README 只说了一半（只强调了 scikit-learn 版本）。

---

## 二、亮点：我认可的东西

### 1. 诚实披露文化（最难得）

我数了一下，你们主动披露的软肋至少有五处：训练/测试 R² 差距（0.99 vs 0.96）、MC Dropout 覆盖率不达标（65–89%）、物理约束只是边界裁剪级而非 PINN、统计特征化丢失空间信息、未自建 CFD 链路。

**做科研的人都知道，主动说出自己的软肋，比被评委当众戳穿要高明一个数量级。** 你们把"答不出口径，比数字低一点致命得多"写进了自己的文档——这句话我原样送给所有研究生都不过分。

### 2. 可复现性工程

复现脚本、seed 固定、数据落库、模型落库、requirements 锁版、README 附一键复现代码、API 带 `r2_evaluated_on` 口径字段。**这一套是我实验室的标准，不是本科生的标准。** 尤其是"发现旧数字 0.9861 不对应任何划分就主动改掉"这件事（pressure-test 附加问），说明你们真的跑过自己的数字，而不是抄来的。

### 3. "宁可拒绝，不给错误答案"的工程底线

输入超出训练分布返回 422、冷启动提示、错误兜底、sweep 端点拒绝外推。**代理模型外推不可信，这是最容易被忽略却最致命的一点，你们做得对。** 包括网站上"~100,000×"用保守表述、不吹 10⁶，这种克制是加分项。

### 4. 对照实验的纪律

基线 MLP vs 残差模型、统计特征 vs 点云融合、纯点云 vs 融合——而且**纯点云 η=0.6076 这么难看的结果你们也如实记录了**。对比实验里有"失败"结果才可信。融合模型两次运行的 R² 区间（0.9718–0.9902）如实报告，没有只报最好的一次。

### 5. 平台完成度

全栈、部署、双语、3D、devlog 逐日可溯、术语统一表、外行试讲清单、汇报一页纸——**这不是一个"模型 demo"，是一个"平台"**。你们甚至把 NASA 原始文献（TP-1337、TM-107310、TM-106711）放进 docs 当参考文献，这个细节说明你们知道做学问要追原始出处。

---

## 三、质疑：我不会因为你是大二学生就放水

### 问题一（最重）：代理模型的自证循环——"最优设计"缺一条 CFD 腿

首页大字写着"**NSGA-II 找到的设计方案超越了全部 1,000 个 CFD 训练样本**"，但细看口径：这个"超越"是**代理模型自己评出来的**。优化用代理模型打分，结论用代理模型验证——这是一个自证循环。

η=0.9173 目前是 **surrogate 的信念**，不是物理事实。R²=0.9561 意味着效率预测仍有约 4–5% 的方差没解释，而你们宣称的效率增益（+5.40%）与代理误差在同一量级。Pareto 前沿最外沿的解恰好是代理模型外推风险最高的区域——优化器会"钻代理模型的空子"，这是代理辅助优化领域人尽皆知的坑。

你们 README 里写了"最终方案仍需 CFD 校验"，方向是对的，但**结论和证据之间的位置关系摆反了**：大字结论放在首页，小字 caveat 放在文档深处。P4（SU2 校验）是你们自己规划的正确答案——**跑出 top-5 解的真 RANS 数字之前，网站的措辞应该从"AI 找到的最优设计"改为"代理模型预测的最优设计"。**

### 问题二：74 维"设计空间"的可实现性

NSGA-II 确实在 74 维上优化（这个不虚），但这 74 维是**叶片表面场量的统计特征**，不是 CAD 参数。特征空间里的 Pareto 前沿 ≠ 几何空间里存在的叶片：

- 一个任意的 74 维向量（哪怕每维都在训练范围内）未必对应任何真实叶片——mean/std/skew/kurt 的组合可以物理上自相矛盾；
- 你们自己披露了统计特征化丢失空间分布信息，那优化解的空间分布信息就是**被丢掉的**——优化器恰恰可能利用这一点找到"统计上像好叶片、几何上不存在"的解；
- 所以"超越全部训练样本"的另一个隐含前提是：这些特征解能**反解回**真实几何。P3 生成式模型是正确答案，但还没闭环（/generate 页当前走的是近邻 + L-BFGS-B 逆设计，不是生成模型）。

**判断标准**：哪天你们能展示"Pareto 解 → 生成/重建出叶片几何 → RANS 验证"，这个闭环就成立了。在那之前，这是课题最该补的一课。

### 问题三：数据集口径——官方 test split 没用上

PLAID Rotor37 官方带一个 **200 组的 test split**（`train_1000` / `test`），你们用的是在 1,000 组内随机 90/10 切。这有两个后果：

1. 你们的"留出测试集"与官方测试集**不可比**——以后任何文献用官方 test 报数字，你们的 0.9844 就没法直接对照；
2. 官方 test 的几何/工况分布与 train 可能不同，随机切分容易高估泛化（同分布采样）。

建议：**在官方 test split 上再报一组 R²**（哪怕模型不重训），这组数字会让你们的结论硬一个档次。另外，`FEATURE_STATS` 越界保护用的是全量 1,000 组（含你们自己的测试集）的范围，而文案写"训练数据范围"——措辞与实现不完全一致，建议统一口径（这个是小问题，但评审问到了要能答）。

### 问题四：NSGA-II 复现的版本敏感性（我踩的坑，写给你们）

如第 1 节所述，同样的 seed 42、同样的代码，换 pymoo/numpy/onnxruntime 版本，η 在 0.9173–0.9212 之间漂。这说明"可复现"目前**依赖完整环境锁版**。改进建议：

- README 复现段从"注意 scikit-learn 必须 ==1.7.2"升级为"**必须完整安装 requirements.txt 全锁版环境**，任何版本偏离都会改变 NSGA-II 结果"；
- 进一步可以做 CI 复现作业（GitHub Actions 里跑一遍 R² + NSGA-II，输出期望值），把"可复现"变成持续集成的检查项——这是最省事也最有说服力的做法。

### 问题五：UQ 的"生产模式"名不副实

`/predict` 页面的 UQ 开关，生产 API 实际用的是**预计算常数 σ**（`n_mc_samples` 参数被接受但不生效）。README 里披露了，这一点我认可；但 API 的请求参数、响应字段（`mode: "uncertainty"`、`n_mc_samples: 100`）会让调用者以为在实时做 MC Dropout。建议接口层面明确返回 `mode: "statistical"`，别让诚实披露只停留在 README 里。

### 问题六：时间线的叙事口径

173 个提交、39 个"开发日"、压缩在 **2026-07-30 到 08-01 三个日历日**里。Git 历史是真实的（SHA 可溯），但"Day N"是工作单元计数，不是自然日——devlog 里写"Day 1–37 跨 07-30→07-31"这个表述，评审一眼就能看出日历对不上。这不是问题，AI 辅助开发是时代红利，你们 pressure-test Q4 的答法也正确（"AI 辅助 + 本人独立完成"）；但**汇报时口径要主动讲清楚**：这是 39 个高强度工作单元，不是 39 天。主动说，比被问出来好——这和你们自己"诚实披露"的原则是一致的。

### 问题七（管理层面）：HANDOFF.md 不该在公共仓库里

我翻仓库时看到 `HANDOFF.md`——你们的内部交接文档，里面写着"宋老师会翻 GitHub""评审团名单""五条铁律"（包括给 AI agent 的 PR 操作纪律）等**内部作战信息**。文件头部自己也写了"不要提交、不要推到 GitHub"，但 v6 更正说它已被 git 跟踪。作为评审我看到了——内容本身不伤人，但**"内部文档出现在公共仓库"这件事显得不专业**。建议：`git rm --cached HANDOFF.md`，归档到本地，别让下一届学弟学妹围观你们怎么给宋老师准备话术（笑）。

---

## 四、评分

按两个尺子分别给：

### 尺子一：本科二年级独立课题（你们的主场）

| 维度 | 分（/10） | 一句话理由 |
|---|---|---|
| 数据与特征工程 | 8.5 | 74 维统计特征路线清晰、代价披露诚实；未用官方 test split 扣分 |
| 代理模型与验证 | 8.5 | R² 全部 >0.95 且逐位可复现；残差结构+物理约束是对小样本的正确直觉 |
| 不确定性量化 | 7.0 | 方法本身弱（MC Dropout 已知局限），但**承认得漂亮**，校准路线（P2）方向正确 |
| 多目标优化 | 7.5 | 真 74 维 NSGA-II、约束合理、可复现；缺 CFD 验证腿 |
| 工程与部署 | 9.5 | 全栈+部署+双语+兜底，接近工业级 demo |
| 可复现性 | 9.5 | 我给研究生改论文都没见过这么齐的复现配套 |
| 科学诚实度 | 9.5 | 五处主动披露，本课题最大加分项 |
| **综合** | **8.7** | **优秀**。作为大二独立课题，超出我对这个阶段的预期 |

### 尺子二：正经科研项目 / 论文标准（如果这是你的硕士课题）

**6.5–7.0 / 10**。差距不在工程，不在复现，而在**验证闭环**：代理模型自证循环未破、特征空间解的可实现性未证、官方基准未对照。把第 3 节问题一、二、三补上，这个分数会显著上升——而且这三件事没有一件是"重新发明"，都是把已有的 P3/P4 做完。

---

## 五、下次见面我会问的三个问题

1. **"你跑过真 RANS 吗？"** —— top-5 Pareto 解的 SU2 验证数字是什么？哪怕只验证一个点，回答"代理预测 η=0.9173，RANS 复核 η=0.90x"就比任何 R² 都有说服力。
2. **"你的 Pareto 解能造出来吗？"** —— 74 维特征向量反解回叶片几何的闭环在哪？P3 生成模型什么时候真正接上 /generate 页？
3. **"官方 test split 上你的模型是多少？"** —— 在 PLAID 的 200 组官方测试集上重测 R²，敢不敢报？

---

## 六、给承泽的话

承泽，最后说几句题外话。

你做了一件很多研究生都不一定做得到的事：**把"数据 → 模型 → 不确定性 → 优化 → 平台"这条 MDO 链路完整地走通了，并且让每个数字都经得起别人重跑。** 这个项目的骨架是健康的——代理模型做前端筛选、UQ 诚实标注、优化交给 NSGA-II、最终留给 CFD 裁决，这个架构判断是对的，方向感很好。

但你要清醒：**目前平台最亮眼的三个数字（0.9844 / 0.9173 / 21.74）全部来自代理模型自己的世界。** 你真正缺的，是让这个代理模型世界和物理世界对一次表。P4 的 SU2 就装在你本机了——把它跑起来，哪怕只验证 5 个解。那一天你的项目会从"一个非常优秀的 demo"变成"一个有科学结论的工作"。

我等你把 RANS 数字发给我。到时候我们再聊。

—— 宋立明
2026-08-03
