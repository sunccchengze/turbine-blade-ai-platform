{
  "name": "qwen-mm-plugins-example",
  "version": "1.0.0",
  "description": "Qwen-MM-Plugins example — a minimal template capability (skill + MCP server) demonstrating the three tool return shapes (text / image / frames); copy it to add your own.",
  "skills": ["./skill"],
  "mcpServers": {
    "qwen-mm-plugins-example": {
      "command": "uvx",
      "args": ["--from", "qwen-mm-plugins[example] @ git+https://github.com/QwenLM/Qwen-MM-Plugins.git@main", "qwen-mm-plugins-example"]
    }
  }
}
