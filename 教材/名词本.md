# 名词本

每个词的法定一句定义以**首次出现那一章的「先认词」表**为准。这里只做索引，避免各章各写一套。

读法：卡住一个词，先回该章六层，不要只背这一页。

| 词 | 首次讲清 | 仓库落点 |
|---|---|---|
| 压气机 / 转子 / Rotor 37 / 跨音速 / 马赫 | U01 | `data_card.md` |
| π η ṁ / 总压 / 等熵 / 质量流量 | U01 | `evidence/metrics.json` 的 r2 与端点 |
| 代理 / 留出集 / ONNX / SU2 / PLAID | U01 | `backend/models/surrogate_model.onnx` |
| Ω、U、C、W、相对马赫、欧拉功、背压 P | U02 | PredictPage 背压滑块 |
| 激波、附面层、SWBLI、喉部、分离 | U03 | `教材/figures/U04-shock-bl.svg` |
| CFD、RANS、残差、relrms、null、一阶迎风 | U04 | `su2_coarse`，`decision_metrics` |
| 74 维、std、偏度、设计变量、CST/FFD | U05 | 特征 CSV |
| 残差块、ReLU、Dropout、PINN（不是） | U06 | notebook 04，ONNX |
| R^2、MAE、3×、决策指标 | U07 | `split`，`decision_metrics` |
| MC Dropout、启发式带、常数 σ | U08 | `mc_dropout_heuristic` |
| 覆盖率、加宽、可交换 | U09 | `calibrate_uq_p2.py` |
| 支配、NSGA-II、0.9173、示意 3D | U10 | `pareto_front_solutions.csv` |
| E0–E4、冻结、越档、禁词 | U11 | `claims.yaml` |
| MDO、三块铁证、TNO、加点、Haftka | U12 | 发送包第 5 节 |

口误对照：基波→激波；负面层→附面层；流出集→留出集；SUR→SU2；方差（八统计里）→std；进口总压（滑块 P）→背压。
