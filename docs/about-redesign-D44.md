# About 页重构方案（D44）

> **2026-08-13 已落地** `AboutPage.jsx`。学长七条已写入：不叫 MDO、74 维不是设计变量、启发式 UQ、边界正则不是 PINN。首页仍是旧口径。

> 给实现用，不是给郭老师发的文。页面是 `frontend/src/pages/AboutPage.jsx`，导航代号 `07 / 关于`。
> 读者顺序：郭老师三分钟扫一眼 → 六个月后的自己 → 路人。
> 视觉继续 D43：Control Room 骨架 + Rotor Editorial 排版。不写独立立项，不写科研成果，不点宋老师。

---

## 1. 现在这页为什么不能给郭老师看

现稿标题是「独立科研与完整工程记录 / Built Independently from Problem to Code」。副文是「独立立项」「完整科研生态」「43 天攻坚」。这和已经改过的暑期信打架：信里说暑假小尝试，页上还在摆架子。

具体有病的句子：

| 现稿 | 问题 |
|---|---|
| 独立立项研发 / 独立科研 | 不是立项，是兴趣小尝试 |
| 本科二年级 | 大一升大二 |
| KIT 303 秒占了缘起整栏 | 新闻引子，不是课题；郭老师不吃这个 |
| 完整科研攻坚轨迹 | Day 日志不是课题史 |
| ~100,000× 相对 3D CFD | 口径不含 1000 组离线样本，About 上太冲 |
| clone `-b arena/019feb03-...` | 当前会话是 `019ff6c7` |
| 底部「独立立项 · 严守学术诚信 · 第一性原理」 | 金句，删 |
| Rocket / Flask 图标 | 装饰，换编号即可 |

郭老师若点开 About，他要看到的是：你知不知道自己停在他那条线的哪一档。现稿完全没写多保真、加点、TNO，只写自己做了 43 天。

---

## 2. 这一页要回答的四句话

D43 规定每个首屏必须回答任务、证据、下一步、不能下的结论。About 的四句定为：

1. **这是什么**：暑假搭的 Rotor 37 标量代理筛选站，浏览器里就能跑。
2. **证据到哪**：留出集 R² 是 E2；Pareto 是代理候选；SU2 粗网格是 E3 趋势；细网格没跑完。
3. **在学科史上停在哪**：停在「三个数的代理」，还没到郭老师现在的场算子和加点。
4. **下一步想做什么**：把 η 覆盖率 65% 当成加点传感器，几何改 CST/FFD。

KIT、Day 01–43、复现命令，全部降为第二屏以后。

---

## 3. 信息架构（自上而下）

```text
07 / 关于
├── A  题头          一句话身份 + 一句话定位          8 行以内
├── B  我停在哪       四档课题史，第三档高亮「这里」     首屏必须看见
├── C  这个站是什么   七个工作台 + 四个可复现数字        双栏
├── D  不能写成的话   三条红线（E2/几何/5.4%）         短
├── E  下一步         α：校准加点，读他的哪两篇         短
├── F  工程日志       Day 01–43 压缩成四条，降级        次要
└── G  复现           分支改 019ff6c7，三行命令         页脚前
```

A+B 必须在 1080p 首屏里。F 可以折起来或缩成细表，不要再占半页发光卡片。

---

## 4. 各块文案定稿（可直接贴进 JSX）

### A. 题头

眉题：`07 / 关于 · ABOUT`

主标题不用「独立科研」：

```text
一次暑假小尝试
A summer filter, not a closed design loop
```

右侧 40 字以内：

```text
孙承泽 · 交大能动强基 2501。载体是 NASA Rotor 37 压气机，不是涡轮。
站在浏览器里跑，代理筛路，物理还没定音。
```

遥测条：

```text
LOCAL WASM · Rotor 37 / PLAID · 证据停在 E2 · 下一步：加点
```

删：D43 STAGE、开源透明 · 全套可复现、本科二年级。

### B. 我停在哪（本页主菜）

四行表，左编号、中年代、右一句话。第三行用暗金左边线，表示「我在这里」。不要用火箭图标。

| 档 | 年代 | 一句话 |
|---|---|---|
| 01 基准 | 1970s–90s | NASA Rotor 37 成为跨音速压气机验 CFD 的公共题。 |
| 02 少样本设计 | 2010s | 伴随、叶型参数化，再是 Kriging / EGO / 多保真。问的是：少算几次真计算，空间能不能探清。 |
| 03 标量代理 | 我现在 | 1000 组公开 CFD → 74 维统计 → 三个数（π, η, ṁ）→ 离线 NSGA-II。网页能拖，叶子还不能拿去加工。 |
| 04 场与加点 | 2024–26 | 神经算子先报 T, p, ρ 再报性能；主动学习用 100–200 次高保真改可制造几何。这是我要往上走的一档。 |

表下补三行，点名但不拍马：

```text
02 这一档里，多保真该不该掺低保真、下一个真样本落在哪，是郭振东老师公开工作里反复问的问题。
04 这一档里，同一转子上已经有先报场再报性能的算子。我还没有场，也还没有加点闭环。
所以这个站的位置很清楚：筛选器。不是设计器。
```

不写 TNO 缩写堆砌，不写 Haftka 全名论文题（About 不是综述）。要核对的人去信和 `docs/guo-line-and-next-path.md`。

### C. 这个站是什么

左栏七个工作台，一行一个，带路由，不要徽章墙：

```text
00 总览     证据分档，E2 / E3 写在脸上
01 预测     74 维滑块 → π, η, ṁ，0.23 ms，本地 WASM
02 探索     两维切片，400 点扫完
03 优化     100 个代理非支配点，不是 CFD 最优
04 不确定度  η 名义 95% 区间只盖住 65%
05 生成     库内近邻，不是新叶型
06 方法     数据怎么切、损失怎么写、粗网格为什么停
```

右栏四个数，口径写在数字下面：

| 数 | 标签 | 必须跟的半句 |
|---|---|---|
| 0.9844 / 0.9561 / 0.9827 | 留出集 R² | n=100，seed=42，训练未见 |
| 0.9173 | 代理最高效率 | 相对训练均值约 +5.4%，未用 CFD 核 |
| 65% | η 区间覆盖率 | 没去拉宽带子凑 95% |
| −3.39 | 粗网格残差平台 | E3 趋势，14 万单元，一阶迎风 |

删掉看板里的 100,000×。要写加速，只写「单点前向 0.23 ms，不含离线 1000 组 CFD」。

### D. 不能写成的话

三条，左编号右一句，黄色只给这一块的边：

```text
01  图上 100 个点叫代理候选，不叫已验证最优叶片。
02  74 维是场的统计量，不是 CST / FFD 那种能进求解器的设计变量。
03  5.4% 不能拿去和 CFD 核过的 0.8%–1.7% 比谁高。
```

### E. 下一步

一块，不要「展望未来」标题：

```text
下一件事
把效率通道覆盖率只有 65% 当成加点传感器。
几何改成 CST 或 FFD。先读多保真「该不该掺 LF」和场算子那两篇，再用几十次收敛 RANS 换一条能对外报的前沿。
场预报跟已有算子学，不另起一套。
```

链到 `/uq` 和 `/methodology`，不链到内部 markdown。

### F. 工程日志（降级）

五条合成四条，标题去「攻坚」：

```text
01–08   数据与标量网络     PLAID 1000×74，残差网，MC Dropout，NSGA-II
09–18   能在浏览器里跑     先走过容器冷启动，再改成 Cloudflare Pages + WASM
19–37   口径收回来         R²、Pareto 数字按可复现脚本对齐，E2/E3 分开写
39–43   几何审计与粗网格   点云拓扑；SU2 14 万一阶停在 −3.39；细网格内存不够
```

布局改成四行细表：`DAY | 六个字 | 一句`。不要 72px+图标+发光底。

### G. 复现

```text
git clone -b arena/019ff6c7-turbine-blade-ai-platform https://github.com/sunccchengze/turbine-blade-ai-platform.git
cd turbine-blade-ai-platform
cd frontend && npm install && npm run dev
```

注明：打开即本地 WASM，不必起后端。模型体检脚本保留一行小字即可。

页脚：

```text
西安交通大学 · 能源与动力工程学院 · 能动强基 2501 · 孙承泽
```

不要导师署名（未进组）。不要「第一性原理」。

---

## 5. 版式（对着现文件改，不大拆壳）

保留：`maxWidth: 1240`、左右 28px、发丝线、`var(--panel)` / `var(--line)`、温黑、青绿色=在跑、暗金=Gate。

改：

- 主标题字号可略收，`clamp(28px, 4vw, 44px)`，不要再撑满「独立科研」那种海报感；
- B 用三线表，不要五张 `card-glow`；
- C 双栏等高，底边对齐，沿用现网格 `1.2fr / 0.8fr`；
- 删 lucide 的 `Rocket`；里程碑若留图标，只用线框圆点；
- 窄屏 `<900px` 单列，表头不换行数字。

About 不做流场装饰。流场在 Explore / Optimize。这里是说明书。

---

## 6. 和别页、和信的口径

| 出处 | 必须一致 |
|---|---|
| 信第 3 稿 | 小尝试；Rotor 37 压气机；5.4% 是代理均值；下一步加点 + CST |
| 首页证据表 | E0–E4 用词不另造一套 |
| `/optimize` | η_max = 0.9173，不是 0.9211 |
| `/uq` | 65% 是伤疤，也是下一步的传感器 |
| `docs/guo-line-and-next-path.md` | About 只写到「停在 03、要去 04」，不写 12 个月甘特 |

公开页不写 Haftka、CLBO、内部文件名。写了像在向路人报师门。

---

## 7. 验收

打开 `/about`，不滚屏能看见：小尝试、Rotor 37 压气机、四档表里「我在 03」、E2。

滚到底应能看见：三条红线、加点下一步、新分支复现命令。

禁止再出现：独立立项、本科二年级、科研成果、完整生态、攻坚、涡扇、宋老师、100,000×、0.9211、019feb03。

---

## 8. 实现切片（批准后再动代码）

1. 只改 `AboutPage.jsx` 文案与本节结构，不动路由和导航字号；
2. 窄屏单列走一遍；
3. `npm run build`；
4. 对照第 7 节逐条勾。

不动 Home / Methodology。About 先收口，避免全站同时改口径。
