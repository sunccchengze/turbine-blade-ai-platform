# 🎯 D40 答辩作战包（项目侧）—— 填坑清单 + 8/4 本机任务 + 自我审查

> ⛔ **时效标记（2026-09-02 追加 · 由分支收敛会话自动判定）** —— 本文件是 **2026-08-08** 的历史快照，**不是现状**。
> 以下写法在今天已经不成立：
> - 第 16 行「| 5 | UQ 生产模式名不副实 | ✅ 已填 | 后端 `mode: "statistical"` + `uq_method` + `note` 字段；`n_mc_samples` 参数说明改为「兼容参数」；mode」→ **引用后端 HTTP 接口——现在数据与模型都是随前端部署的静态 JSON / ONNX**
> - 第 57 行「- 后端必须 SnapDeploy 手动 Redeploy（UQ mode 改动在 `backend/app/`）——不 Redeploy 线上还是旧行为，答辩演示 /predict UQ 时会被看到 `mode:"un」→ **后端 Redeploy：平台已纯前端 WASM，线上无部署中的后端，推 main 之后没有这一步**
> - 第 59 行「- 验证线上：`/health` 200；`/api/predict/model-info` 正常；首页新文案已生效。」→ **引用后端 HTTP 接口——现在数据与模型都是随前端部署的静态 JSON / ONNX**
> - 第 116 行「| 线上后端若未 Redeploy，UQ mode 还是旧的 | 答辩前一晚必须验证线上（任务 3）——这是自己的失误，不是项目缺陷，但被看到很尴尬 |」→ **后端 Redeploy：平台已纯前端 WASM，线上无部署中的后端，推 main 之后没有这一步**
>
> 现行口径唯一来源：`HANDOFF.md`（§0.-1 十一条铁律、§9.5 架构现状）、`docs/BRANCH-SAFETY.md`（会话与 git 纪律）、`evidence/metrics.json`（对外数字）。
> ——以及第二轮：
> - 第 6 行「> - 第 57 行「- 后端必须 SnapDeploy 手动 Redeploy（UQ mode 改动在 `backend/app/`）——不 Redeploy 线上还是旧行为，答辩演示 /predict U」→ **部署拓扑已变：线上只有 Cloudflare Pages 静态站点**
> - 第 8 行「> - 第 116 行「| 线上后端若未 Redeploy，UQ mode 还是旧的 | 答辩前一晚必须验证线上（任务 3）——这是自己的失误，不是项目缺陷，但被看到很尴尬 |」→ 后端 Redeploy：平」→ **部署拓扑已变：线上只有 Cloudflare Pages 静态站点**
> - 第 67 行「- 后端必须 SnapDeploy 手动 Redeploy（UQ mode 改动在 `backend/app/`）——不 Redeploy 线上还是旧行为，答辩演示 /predict UQ 时会被看到 `mo」→ **部署拓扑已变：线上只有 Cloudflare Pages 静态站点**
> - 第 149 行「- 09:30 检查：链接、网络、备份链接（本地 `localhost:8000` 兜底）、手机热点」→ **前端不再有 API 基址：无后端可调，`frontend/.env.local` 那条已无意义**
> **正文一字未改**——当时的判断与过程仍按原样保留，供回顾历程用。

> 目标：8/5 上午 10:00 一小时答辩（宋立明教授）。宋老师 GHG-01 观后感（仓库根 `GHG-01.md`）的 7 条批评全部有下文；
> 已填的坑都有证据，未填的坑都有精确执行步骤。**原则：宁可承认没做完，不可假装做完了。**

---

## 一、宋老师 7 条批评 → 填坑状态表

| # | 宋老师的批评 | 状态 | 证据 / 产物 | 你还要做的 |
|---|---|---|---|---|
| 1 | 代理模型自证循环，缺 CFD 腿 | 🟡 一半已填 | ① 首页措辞已降级为「代理模型预测的最优设计」并注明 RANS 待终审（`HomePage.jsx`）；② `pareto_evidence.py` 证据链（范围合规/流形位置/外推量/邻域误差）；③ README 优化节新增诚实披露 | **8/4 跑 SU2 抽查 top-3**（见 §三）——这是把 🟡 变 ✅ 的唯一路径 |
| 2 | 74 维特征空间解的可实现性 | 🟡 证据已备 | `pareto_evidence.py`：100 解全部 74 维落在全量 1,000 组范围内（0 越界）；但距离证据显示前沿在流形边缘（最近邻中位 17.4 ≈ 训练集内部的 3×），η 超出最近邻实测中位 0.0235（=MAE 的 14×）——**承认外推，用 RANS 裁决** | 答辩话术见《答辩侧》§三-2 |
| 3 | 官方 test split 没用上 | ✅ 已查明并验证 | 官方 test 200 组为**完全黑盒**（几何+输出隐藏，仅 Ω/P，防泄漏设计）→ R² 无法计算；已做等价验证：①官方 train 1000 组与仓库 CSV Ω/P 逐组精确匹配（1000/1000）②test 工况 200/200 落在训练范围 ③近邻标签分布一致（脚本 `eval_official_test_split.py` v3 + README 数据节） | 答辩直接讲黑盒结论（见《答辩侧》Q3），无需再跑 |
| 4 | NSGA-II 版本敏感 | ✅ 已填 | README 复现段升级为全锁版声明（含实测漂移数字：pymoo 0.6.2→0.9212、numpy 2.x→0.9191、ort 1.28→0.9180）；`docs/verify-reproducibility-workflow.yml` CI 模板（Actions 页手动安装） | 若时间够，装一下 CI；不够就只说 README |
| 5 | UQ 生产模式名不副实 | ✅ 已填 | 后端 `mode: "statistical"` + `uq_method` + `note` 字段；`n_mc_samples` 参数说明改为「兼容参数」；model.py docstring 重写 | **部署后验证**：`/api/predict` 带 `include_uncertainty=true` 响应含 `mode:"statistical"` |
| 6 | 时间线口径 | ✅ 已填 | README 进度节新增口径说明（Day = 工作单元计数，完成于 3 个日历日） | 答辩主动讲（见《答辩侧》§四-6） |
| 7 | HANDOFF.md 在公共仓库 | 🟡 等你操作 | 未删（按你要求） | **8/4 执行**：`git rm --cached HANDOFF.md` + `.gitignore` 追加 `HANDOFF.md` + commit + push。注意 git 历史仍可见旧版本，但对评审「当前仓库」已不可见 |

**口径统一提醒**：宋老师原文「第一印象」里 8.7/10 是按「本科独立课题」尺子打的；「论文标准」尺子是 6.5–7.0。
答辩目标不是争 9.0 这个数，而是**证明批评 1/2/3 的闭环正在合拢**——合拢了，分数自然上 9。

---

## 二、8/4 本机任务清单（按杠杆排序）

### 任务 1（✅ 已完成 8/4）：官方 test split 验证（黑盒结论）
> 实测结论：官方 test 200 组是**黑盒**（几何+输出隐藏，仅 Ω/P），R² 无法计算。
> 等价验证已完成并写进 README：
> - 官方 train 1000 组 vs 仓库 CSV：Ω/P 精确匹配 **1000/1000**（数据一致性铁证）
> - 官方 test 200 组工况：**200/200** 在训练范围内（同域）
> - test 工况近邻标签分布与训练一致（π 1.984/η 0.870/ṁ 19.51）
> 产物：`backend/data/processed/official_test_sanity.json` + README 数据节 + 答辩话术（《答辩侧》Q3）。

### 任务 2（🔴 最高杠杆）：SU2 RANS 抽查 top-3 —— 3–5 小时（成败都记录）

目标：**跑通至少 1 个算例**，得到「代理预测 vs RANS 实测」对照表。定位是**相对趋势验证**（你们 D38 文档的既定口径：定量对不上属正常，写讨论点）。

- 你的环境已就绪：SU2 v8.5 原生版（`C:\SU2-extracted\bin\SU2_CFD.exe`）+ 配置调试完成（见 `docs/day39-battle-report.md` §六）。
- 步骤建议：
  1. 从 `pareto_front_solutions.csv` 取 top-3（最高 η、最高 ṁ、折中解）；
  2. 用现有 Rotor37 网格（PLAID 是 CGNS，需转 SU2 格式——若转换未就绪，先用 SU2 自带 turbomachinery 教程网格跑通流程）；
  3. 工况设训练范围内：Omega≈1790 rad/s、P≈3.59e5 Pa（与 Pareto 解一致）；
  4. 跑完提取 η/π/ṁ，填入 `backend/scripts/run_su2_validation_p4.py` 生成的 comparison 结构；
  5. 无论结果如何，记录 3 行对照表 + 1 段「差异讨论」（为什么对得上/为什么对不上：网格、湍流模型 SA、定常 RANS vs 数据集的求解器差异）。
- **如果跑不通（网格转换没搞定等）**：不要硬熬超过 4 小时。答辩话术 = 「SU2 v8.5 环境已就绪、配置调试完成、6 算例准备脚本已就绪；RANS 抽查是 8/10 前第一优先级」——宋老师要的是「你在做，而且有明确路径」，不是「你吹你做完了」。

### 任务 3（🟡）：清理 + 部署 —— 30 分钟

```bash
git rm --cached HANDOFF.md
echo "HANDOFF.md" >> .gitignore
git add -A && git commit -m "D40: remove internal HANDOFF from repo" && git push
```

- 然后把 `arena/019fc539-turbine-blade-ai-platform` 合进 main（在 GitHub 网页操作，PR 合并）。
- **后端必须 SnapDeploy 手动 Redeploy**（UQ mode 改动在 `backend/app/`）——不 Redeploy 线上还是旧行为，答辩演示 /predict UQ 时会被看到 `mode:"uncertainty"`。
- 前端 Cloudflare Pages 自动部署（跟 main）。
- 验证线上：`/health` 200；`/api/predict/model-info` 正常；首页新文案已生效。

### 任务 4（🟢）：全量复现核对 —— 20 分钟（答辩前夜的定心丸）

```bash
python -m venv .venv && source .venv/Scripts/activate   # Windows
pip install -r backend/requirements.txt
# R²：
python - <<'EOF'   # README §3 的脚本，期望 0.9844/0.9561/0.9827
...（照 README 快速复现 §3 抄）
EOF
python backend/scripts/generate_pareto_evolution.py      # 期望 max η=0.9173 max ṁ=21.74（全锁版）
python backend/scripts/pareto_evidence.py                # 证据链报告重新生成
```

> ⚠️ 只在这台**锁版环境**上跑；如果答辩演示在别的机器，先装 requirements.txt 全量锁版。
> 宋老师问「给我现场跑一下」时，你只需展示 README §3 脚本 + 输出——10 秒出数。

---

## 三、SU2 冲刺速查（任务 2 的细化）

| 项 | 内容 |
|---|---|
| SU2 路径 | `C:\SU2-extracted\bin\SU2_CFD.exe`（v8.5.0 "Harrier"） |
| 配置坑（已踩） | 中文注释 ❌；`#` 注释 ❌；v8.5 选项名更新：`SPECIFIC_HEAT_CP` / `MG_SMOOTH_OUTPUT` / `BC_EVAL_FREQ`（见 day39 战报 §六） |
| 网格 | PLAID CGNS → SU2 需要转换；若未就绪用 SU2 教程网格先跑通流程 |
| 工况 | 用 Pareto 解的 Omega/P（~1790 rad/s, ~3.59e5 Pa），在训练范围内 |
| 输出 | 3 行对照表（代理 vs RANS：η/π/ṁ）+ 差异讨论 |
| 期望差幅 | 定性对得上（η 高→RANS η 也高、相对排序一致）即为成功；绝对偏差 >0.02 要写原因（网格/湍流模型/求解器差异） |

**答辩页建议**（若跑出数字）：PPT 第 18 页放对照表 + 一句「相对趋势一致/偏差来自网格与湍流模型」；
若只有「环境就绪 + 计划」，该页改为「RANS 终审计划」流程图。

---

## 四、自我审查：改完之后，这个项目还剩什么亮点、什么可批评？

> 答辩前一天晚上重读这一节——宋老师可能问的，都在这。

### 改完后新增的亮点（要主动说）

1. **证据链自查成为可复现脚本**：`pareto_evidence.py` 把「范围合规 → 流形位置 → 外推量 → 邻域误差」做成了一键报告——宋老师批评一、二的数据版回答，而不是嘴硬。
2. **官方基准验证（已跑）**：官方 train 1000/1000 Ω/P 精确匹配 + test 200/200 同域 + 近邻标签一致——"官方 test 黑盒"的正确应对，敢查、敢报、口径清晰。
3. **版本敏感性文档化**：把「seed 42 只在锁版环境成立」这个坑写进 README，并给了 CI 模板——可复现性从口号变成工程。
4. **UQ 接口诚实化**：`mode:"statistical"`，接口层与 README 口径一致。
5. **首页措辞降级**：「代理模型预测的最优设计」——把 caveat 从文档深处提到结论本身，这是最难的诚实。

### 改完后仍然可批评的点（自己先想好答法）

| 可批评点 | 诚实答法 |
|---|---|
| RANS 还没出数字（若任务 2 未跑通） | 「SU2 环境已就绪、链路已通、6 算例脚本已就绪；抽查是 8/10 前第一优先级。我选择不编数字，因为您教过口径比数字重要。」 |
| P2 校准（93.5–96.5% 覆盖率）只在 100 组留出集上评估 | 承认小样本局限；conformal 的 coverage 保证是渐近的，100 组有波动；下一步在官方 test（200 组）上重测 |
| fused 模型 0.9902 是单次训练结果，训练有随机性 | 如实：README 已给区间（0.9718–0.9902）与两次运行记录；未做多 seed 平均是 future work |
| /generate 页是近邻 + L-BFGS-B 逆设计，不是生成模型 | P3 条件扩散代码已完成、真数据链路已通，页面上线；「生成模型接管逆设计」是已规划未完成 |
| 3D 叶片是参数化示意，不是真实 CFD 网格 | 明说是「参数化几何可视化」，用于 Pareto 解的直观对比；真实网格在 P4/数据链路中 |
| 线上后端若未 Redeploy，UQ mode 还是旧的 | 答辩前一晚必须验证线上（任务 3）——这是自己的失误，不是项目缺陷，但被看到很尴尬 |
| 官方 test 是黑盒，没报 R² | 已查明：几何+输出隐藏是数据集设计（防泄漏）；等价验证三项已跑（train 1000/1000 匹配、test 200/200 同域、近邻标签一致），README 已写清——这是比"硬报一个 R²"更专业的应对 |
| SU2 配置里 MACH 0.4 / SA 是教程默认值，与 Rotor37 真实工况（跨音、16188 rpm）有差距 | RANS 结果只做相对趋势解读，不做绝对精度声称；写进差异讨论 |
| 平台「多学科」名实差距 | 目前只有气动学科（π/η/ṁ），结构/热学科未接入；多学科=愿景，气动=当前载体，README 定位已写清楚 |

---

## 五、8/4 – 8/5 时间线（贴墙版）

**8/4（优化日）**
- 07:30 起床 → 任务 1：官方 test split（下载期间同时做任务 3 的清理+部署）
- 10:00 任务 2：SU2 冲刺（最多 4 小时，跑通或记录卡点）
- 15:00 合并 main + Redeploy 后端 + 线上验证（新文案、mode:statistical、health）
- 16:00 任务 4：全量复现核对（R² / NSGA-II / pareto_evidence）
- 17:00 更新 README（官方 test 数字若有）+ 本文件勾掉完成项 → commit + push
- 18:00 洗澡吃饭，**晚上不碰项目代码**

**8/4 晚 – 8/5 早（答辩准备日）**
- 19:00–21:00 做 PPT（按《答辩侧》大纲，24 页）
- 21:00–22:30 对着讲稿过第 1 遍（计时，40 分钟）
- 22:30 睡觉（睡够，答辩是体力和脑力活）
- 8/5 07:30 打开线上站（唤醒后端，看 WakeUpBanner 是否消失）
- 08:30 完整彩排第 2 遍（40 分钟讲 + 自问 3 个必问问题）
- 09:30 检查：链接、网络、备份链接（本地 `localhost:8000` 兜底）、手机热点
- 10:00 答辩开始。**开场 30 秒内讲清楚「我今天要证明什么」**

---

## 六、开场白（30 秒，逐字稿）

> 「宋老师好，谢谢您的时间。今天这一个小时，我想向您证明三件事：
> 第一，这个平台的所有数字都可以当场复现——不是截图，是脚本；
> 第二，我知道这个项目最脆弱的地方在哪——代理模型的自证循环，以及特征空间解的可实现性，
> 而且我已经用证据链自查过它，并用 RANS 抽查来裁决它；
> 第三，我清楚它距离一个真正的科研结论还差什么——以及我接下来一个月怎么补。
> 我们开始。」
