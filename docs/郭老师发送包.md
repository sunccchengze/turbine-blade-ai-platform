# 给郭振东老师的发送包（第 4 稿检查单）

检查日期：2026-08-13。公开数字只对 `evidence/metrics.json`。

## 1. 现在能不能发

**信本身：可以发。** 从 `docs/暑期总结-致郭振东-发送正文.txt` 全选粘贴即可。

主题：

```text
孙承泽｜暑期一个小尝试（Rotor 37）
```

不要附：HANDOFF、40 分钟讲稿、frontier 对照（里面点了宋老师）、教材、本检查单。  
可以附：什么都不附。网址已经在信里。

线上站 `https://turbine-blade-ai-scz.pages.dev/` 若还没吃到本分支，首页仍可能写 `~100,000×`、预测页仍可能把 P 写成进口总压、优化页仍可能写「多学科权衡」。这些已在本分支改掉。发出前请先打开线上站核对下面第 4 节；对不上就先部署本分支，或在信里加一句「关于页和优化页请以我这封信的口径为准」。

## 2. 第 4 稿相对第 3 稿改了什么

| 第 3 稿 | 为什么不能留 | 第 4 稿 |
|---|---|---|
| 九组量各取均值、**方差**、分位数 | 八个数里是 **std**，不是方差；也不止这三个 | 八个统计量写全 |
| 滑块上的 P = **进口总压** | 数据卡与术语表是**背压**；页上也已改口 | 转速和背压 |
| 「接了欧拉做功估滞止焓升」像写进了网络 | 那只是页面用等熵关系估的参考数 | 写明：页面参考；网络只有边界惩罚；不是 PINN |
| 三维「安装角拧着」像真叶子 | 示意重构，不是 74 维反解 CAD | 写明示意 |
| 末段保研 / 时代淘汰 / 博士后出国当老师 | 和「暑假小尝试」打架，也像要立项 | 收成：每周 4–6 小时；想读 2018 SMO 和 2025 TNO；CST；加点 |

未改、且经核对仍成立：

- R² 0.9844 / 0.9561 / 0.9827，n=100，seed=42  
- η_max 0.9173，+5.4% 相对训练均值 0.8703  
- ṁ_max 21.74 kg/s，该点 η≈0.873  
- π_max 2.107  
- 覆盖率 89% / 65% / 88%  
- SU2 粗网格 relrms=−3.39，未收敛  
- 不写 MDO、不写可制造 Pareto、不点宋老师、不写涡扇、不写 INTJ、不写瑞士网格  

汉字约 1768，落在「约 2000 字」里。

## 3. 红线自检（信的正文）

- [x] 不称 MDO  
- [x] 不称可制造最优叶片  
- [x] 不称校准的 95% CI  
- [x] PINN 只作为「不是」出现  
- [x] Rotor 37 = 压气机转子  
- [x] 5.4% 标明代理相对训练均值，并补了流形 3×  
- [x] 加速比不写 100,000×；写明不含离线 1000 组 CFD  
- [x] 不把 conformal 93.5–96.5% 写进去  

## 4. 请他点开时的推荐顺序

1. 关于页 `/about`：四档里第三档是 HERE。  
2. 优化页 `/optimize`：流量–效率散点。点最大流量附近，看 η 掉到约 0.873。右边三维是示意。  
3. 不确定度页 `/uq`：η 带子最窄、覆盖率 65%。  
4. 预测页 `/predict`：P 应显示**背压**。  

不要主动点生成页当「新叶型」。信里已经说了是库内检索。

## 5. 他若回信，用这些句子，不要临场加码

| 他可能问 | 你答 |
|---|---|
| 这是不是优化出一片好叶子？ | 不是。0.9173 是特征空间里代理吐的数。没有 CST/FFD，也没有收敛 RANS。 |
| R² 0.98 为什么还说不可靠？ | 留出集平均拟合。Pareto 在流形边上，大约 3 倍训练内部距离。排序对不对，要等 Top-k RANS。 |
| 65% 是不是算错了？ | 不是。名义 95% 的启发式带，η 只盖住 65%。生产端还是常数 σ。 |
| 和我 2025 年 TNO 比呢？ | 您已经在子午面上先报场再报性能。我还停在三个标量。我想跟的是加点，不是另做一套算子。 |
| 5.4% 比季路成组的 1.2% 高？ | 不能比。他们的是 CFD 核过的。我的是代理相对训练均值。 |
| 下一步做什么？ | 几何改 CST；覆盖率当加点传感器；几十次收敛 RANS 核几个点。 |
| 要不要进组 / 立项？ | 先把 2018 SMO 和 2025 TNO 读完，再听您的。这封信不是立项申请。 |
| 网页上的 100,000×？ | 只比单点推断和单点 RANS，1000 组离线 CFD 没算进去。不当贡献。 |
| 三维是真叶子吗？ | 示意。74 维反解不出合法 CAD。 |
| P 是进口总压吗？ | 不是。是背压。旧页签错过，已经改。 |

## 6. 仍未部署、但本分支已改的页面

- 预测页：P 从「进口总压」改为「背压」。  
- 优化页：去掉「多学科权衡」；假超体积数字改为破折号。  
- 首页：顶栏不再把 ~100,000× 当主数字；生成入口改为「库内近邻检索」；ONNX 体积改为 2.11 MB。  

线上 Pages 同步后，再点一遍第 4 节。

## 7. 发出后

把发出日期记在 `docs/DEFENSE_QA.md` 或本文件末尾。他回了什么，原话另存，不要靠聊天记录。
